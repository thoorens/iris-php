{php}$icone = $this->crudIcon(){/php}
<h2>Edit a screen in <i>{category}</i></h2>
<div>
    {(icone->create(10))} Create a new screen
    <ul class="crud">
        {foreach(screens,screen)}            
            <li>
                {(icone->setData($screen)->update())}
                {(icone->delete())}
                {(screen->Description)}
            </li>
            {endforeach}
        </ul>
    </div>