<?php
/* The Files are used during operation of CLI commands:
 * 
 * -- SQL : contains files that permit to create a small SQLite file containing error messages and help screens (presently only in French and English versions) used during execution
 *    --- of the CLI command
 * -- application : some files created in the application directory during a --createproject command execution (no directory tree)
 *    ----------- 
 * -- public : some files places in the public directory during a --createproject command execution 
 *    ------  
 * -- database : some files used to realize a small database management system
 *    --------
 */
